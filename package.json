{
  "name": "hdcredit",
  "private": true,
  "version": "1.0.0",
  "description": "Pruebas automatizadas de HD Crédit (la app en sí sigue siendo index.html plano, sin build).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "http-server": "^14.1.1"
  }
}
